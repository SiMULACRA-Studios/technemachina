# v0.4.11a — Synapse Map Recovery Confirmed

Date: 2026-06-11

## Status

Synapse Map constellation renderer is restored and healthy.

## Confirmed Diagnostic

scripts/synapse_doctor.py passed on 2026-06-11 at 08:07.

Confirmed:

- 257 nodes
- 675 edges
- backend /synapse/map online
- backend /synapse/analysis online
- frontend index.html served
- frontend main.js served
- no dangerous relative /synapse/map fetch
- no dangerous relative /synapse/analysis fetch
- index.html cache-busts main.js

## Root Cause

The earlier black-map failure came from frontend main.js requesting Synapse endpoints from the frontend static server on port 5173 instead of the backend on port 8000.

A second recovery issue occurred around backend startup discipline. The daemon must be launched from the daemon virtual environment using the live app.py FastAPI entrypoint.

Correct backend launch:

    cd ~/Downloads/Technemachina-Daemon-v0.1/daemon
    source .venv/bin/activate
    python3 -m uvicorn app:app --reload --host 127.0.0.1 --port 8000

## Correct Endpoints

- Map: http://127.0.0.1:8000/synapse/map
- Analysis: http://127.0.0.1:8000/synapse/analysis
- Frontend: http://127.0.0.1:5173

## Protected Build Rules

Before changing Synapse Map UI, run:

    cd ~/Downloads/Technemachina-Daemon-v0.1
    python3 scripts/synapse_doctor.py

If the doctor fails, stop and diagnose before patching.

Terminal discipline:

- Terminal 1: backend only
- Terminal 2: patches, tests, doctor
- Terminal 3: frontend only

Do not paste commands into running server terminals.

## Next Build

Proceed next with:

v0.4.11b — Selected Signal Cards

Reason: this improves the Synapse Map meaning layer without risking the recovered constellation renderer.

Do not iframe the Companion into the map.
