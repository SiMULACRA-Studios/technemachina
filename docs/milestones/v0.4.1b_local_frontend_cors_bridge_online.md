# v0.4.1b — Local Frontend CORS Bridge Online

Status: ONLINE  
Date: 2026-06-10  
Mutation policy: Local dev infrastructure only

## Summary

The local frontend dev server can now fetch live daemon API data from the backend.

The Synapse Data Adapter test page served from:

- `http://127.0.0.1:5173`

can successfully fetch:

- `http://127.0.0.1:8000/synapse/map`

## Confirmed

The browser test page now reports:

- `SUCCESS`
- `Node Count: 255`
- `Edge Count: 675`

## Implemented

Cleaned duplicate CORS middleware blocks and installed one unified local CORS bridge allowing:

- `http://127.0.0.1:5173`
- `http://localhost:5173`
- `http://127.0.0.1:3000`
- `http://localhost:3000`
- `http://127.0.0.1:8000`
- `http://localhost:8000`
- `null`

## Doctrine

Local-first development.

Frontend may read daemon API state.

Renderer remains separate from data.

No memory mutation.
