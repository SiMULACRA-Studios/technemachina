# v0.4.2 — Synapse Data Adapter Galaxy Derivation Online

Status: ONLINE  
Date: 2026-06-10  
Mutation policy: Read-only / non-mutating

## Summary

The Synapse Data Adapter now derives galaxy structure from live `/synapse/map` node data when the backend does not provide a top-level `galaxies` object.

## Confirmed Browser Test

The Synapse Data Adapter test page reports:

- `SUCCESS`
- `Version: derived-v0.4.2`
- `Node Count: 255`
- `Edge Count: 675`
- `Galaxy Count: 6`

## Implemented

Updated:

- `frontend/synapse/SynapseDataAdapter.js`

Added adapter functions:

- `inferSynapseNodeKind(node)`
- `inferSynapseGalaxyName(node)`
- `deriveSynapseGalaxies(nodes)`
- enriched `normalizeSynapseMap(rawMap)`
- enriched `summarizeSynapseData(normalized)`

## Boundary

This milestone does not mutate memory.

This milestone does not replace the renderer.

This milestone does not alter the backend map endpoint.

The adapter derives view-ready galaxy structure from existing read-only node data.

## Doctrine

Data first.

Renderer second.

Derived structure is allowed.

Silent memory mutation is forbidden.

Oracle remains the gate.
