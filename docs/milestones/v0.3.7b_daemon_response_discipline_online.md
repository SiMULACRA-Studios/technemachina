# v0.3.7b — Daemon Response Discipline Patch Online

Status: ONLINE  
Date: 2026-06-10  
Mutation policy: Context instruction only / non-mutating

## Summary

The daemon now gives a clean bounded answer about the Synapse Map visual frontend renderer.

Previously, after the Daemon Awareness Bridge came online, the daemon correctly acknowledged runtime project awareness but included an unsolicited external-tool suggestion.

This patch tightened the runtime awareness instruction so the daemon does not mention external tools unless the user explicitly asks for tool suggestions.

## Confirmed Behavior

When asked:

"Do you know whether the Synapse Map visual frontend renderer exists yet?"

The daemon now responds:

"I cannot visually perceive the browser canvas directly, but my runtime project context confirms that the Synapse Map frontend prototype exists and has active renderer work."

## Correct Boundary

The daemon may say:

- it cannot visually perceive the browser canvas directly,
- runtime project context confirms frontend renderer work,
- the Synapse Map frontend prototype exists.

The daemon should not say:

- no renderer work has been registered,
- it can visually see the canvas,
- unsolicited references to Perplexity, Claude, ChatGPT, web search, APIs, or external tools.

## Doctrine

No fake vision.

No stale denial.

No unsolicited tool name-dropping.

Project-aware companion.

Same companion.

Different surface.

Different permissions.

Oracle remains the gate.
